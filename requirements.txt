# Core runtime dependencies for the reservoir IQ-Learn pipeline.
# Tested with Python >= 3.10.
torch>=2.0
numpy>=1.24
scipy>=1.10          # utils/metrics.py uses scipy.stats.pearsonr
pandas>=2.0
optuna>=3.4
pyyaml>=6.0
matplotlib>=3.7
ruamel.yaml>=0.18   # comment-preserving YAML write-back (falls back to PyYAML if absent)
shap>=0.44          # SHAP figures only; results.py skips them gracefully if missing
